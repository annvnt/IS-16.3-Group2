# Runtime
numpy>=1.24

# Exact MIP benchmark; requires a Gurobi license (a free size-limited/academic license
# works for the small instances used here)
gurobipy>=11.0

# Gantt chart plotting (python ffll_vs_mip.py saves figures to gantt_charts/)
matplotlib>=3.7
