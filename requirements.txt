# Runtime
numpy>=1.24

# Visualization (only needed for `python -m ffll_igls.viz`; core runs without it)
matplotlib>=3.7

# Dev / tests (optional — only needed to run `pytest`)
pytest>=8.0

# Exact MIP benchmark (only needed for `python ffll_vs_mip.py`); requires a Gurobi
# license (a free size-limited license works for the small instances used here)
gurobipy>=11.0
